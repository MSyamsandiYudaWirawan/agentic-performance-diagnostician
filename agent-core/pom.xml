<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>io.diag</groupId>
    <artifactId>diagnostician-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>agent-core</artifactId>
  <name>agent-core</name>
  <description>Spring AI agent loop: tools, prompt, ledger, keep/revert (keep-rule v2),
    guardrails, resume (scope §4.1-4.2).</description>

  <dependencies>
    <dependency>
      <groupId>io.diag</groupId>
      <artifactId>evidence</artifactId>
    </dependency>
    <dependency>
      <groupId>io.diag</groupId>
      <artifactId>target-runner</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter</artifactId>
    </dependency>

    <!-- Spring AI lands at build step 8 (scope §10.8 decision checkpoint):
         1) set spring-ai.version in the parent,
         2) uncomment the BOM import below,
         3) add the chosen starter (e.g. spring-ai-starter-model-...).
         Kept commented so step 0's `mvn package` gate stays green.

    <dependencyManagement>
      <dependencies>
        <dependency>
          <groupId>org.springframework.ai</groupId>
          <artifactId>spring-ai-bom</artifactId>
          <version>${spring-ai.version}</version>
          <type>pom</type>
          <scope>import</scope>
        </dependency>
      </dependencies>
    </dependencyManagement>
    -->

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>
