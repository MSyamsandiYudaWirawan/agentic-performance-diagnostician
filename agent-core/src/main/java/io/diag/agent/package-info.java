/**
 * Agent core (scope §4.1–4.2): Spring AI loop, the four tools
 * ({@code runBenchmark}, {@code captureAndAnalyzeJfr}, {@code readSource},
 * {@code applyChange}), system prompt (hypothesis menu H1–H7, investigate
 * before proposing), structured decision schema with falsifiable prediction,
 * hypothesis ledger, keep-rule v2 (RPS-or-p95 classic keeps, verified-mechanism
 * keeps for saddles), guardrails, resume.
 */
package io.diag.agent;
